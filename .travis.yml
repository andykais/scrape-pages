language: node_js

stages:
  - name: typecheck
    if: branch = master OR env(TRAVIS_PULL_REQUEST)
  - name: test
    if: branch = master OR env(TRAVIS_PULL_REQUEST)
  - name: lint
    if: branch = master OR env(TRAVIS_PULL_REQUEST)
  - name: deploy-npm
    if: tag IS present
  - name: deploy-website
    if: branch = master AND NOT env(TRAVIS_PULL_REQUEST)

cache:
  directories:
    - $HOME/.npm

jobs:
  include:
    - stage: typecheck
      node_js: '8'
      script: npm run typecheck

    - stage: test
      node_js: '10'
      script: npm run test:unit
    - stage: test
      node_js: '9'
      script: npm run test:unit
    - stage: test
      node_js: '8'
      script: npm run test:unit

    - stage: lint
      node_js: '8'
      script: npm run lint

    - stage: deploy-npm
      node_js: '8'
      script: skip
      deploy:
        provider: script
        script: bash publish.sh
        skip_cleanup: true
        on:
          tags: true

    - stage: deploy-website
      node_js: '8'
      before_install: cd website
      script: npm run build
      deploy:
        provider: pages
        local-dir: website/dist
        skip-cleanup: true
        github-token: $GITHUB_TOKEN
        keep-history: true
